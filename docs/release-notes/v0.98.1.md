# Release Notes - Luminous v0.98.1

Patch release: fixes signed auto-update artifacts (`latest.json`) not being generated by CI — a missing `bundle.createUpdaterArtifacts` config flag meant v0.98.0's updater plugin wiring had nothing valid to check against.
