[workspace]
members = ["src-tauri"]
resolver = "2"

[profile.release]
panic = "abort"
codegen-units = 1
opt-level = "s"   # optimize for size in release builds

[profile.dev]
opt-level = 0
debug = true
